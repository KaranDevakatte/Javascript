# Closure
Closure = function + uska lexical environment (outer variables access)

Matlab:

Ek function apne outer function ke variables ko yaad rakhta hai, even agar outer function execution finish ho chuka ho.

function + remembered variables = closure