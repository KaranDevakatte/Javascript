# Activity 4 — Function Factory (Real JS Use Case)

Closure use karke multiplier function factory banao.

# Example:

let double = multiplier(2);
let triple = multiplier(3);

console.log(double(5)); // 10
console.log(triple(5)); // 15