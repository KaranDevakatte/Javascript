# Activity 3 — Private Bank Balance

Closure use karke private variable create karo.

# Problem

Bank account system banao:

# Functions:

deposit(amount)
withdraw(amount)
checkBalance()

# Closure variable ko copy nahi karta, same variable reference store karta hai.